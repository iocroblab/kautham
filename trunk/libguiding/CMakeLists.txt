# INCLUDE_DIRECTORIES( ${Boost_INCLUDE_DIRS} )
# SET(Boost_USE_MULTITHREADED ON)
INCLUDE_DIRECTORIES( ${CMAKE_CURRENT_SOURCE_DIR} )
	
FILE(GLOB SRC_FILES *.cpp)
FILE(GLOB HDR_FILES *.h *.hpp *.hxx)

ADD_LIBRARY(libguiding ${SRC_FILES} ${HDR_FILES} )
TARGET_LINK_LIBRARIES( libguiding )   #${Boost_SYSTEM_LIBRARY} ${Boost_THREAD_LIBRARY} ${Boost_SERIALIZATION_LIBRARY})

IF ( UNIX )
  SET_TARGET_PROPERTIES( libguiding PROPERTIES OUTPUT_NAME guiding )
ENDIF( UNIX )
