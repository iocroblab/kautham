files named resultsxxyyzz.m correspond to a cost function:
	rAlpha[i] = (1-abs(beta[i]))*abs(alpha[i]-alpha0);
files named resultsxxyyzzalpha corresponf to cost function:
	rAlpha[i] = (1.0/3.0)*(1-abs(beta[i]))+(1.0/3.0)*abs(alpha[i]-alpha0)+(1.0/3.0)*abs(alpha[i]);

Estas funciones de coste no estn bien