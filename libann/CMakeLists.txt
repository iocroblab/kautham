# LibDNN

INCLUDE_DIRECTORIES( .)
FILE( GLOB SRC_FILES *.c )
FILE( GLOB HDR_FILES DNN/*.h )
ADD_LIBRARY(libDNN ${SRC_FILES} ${HDR_FILES} )
SET_TARGET_PROPERTIES(libDNN PROPERTIES LINKER_LANGUAGE C)

IF(UNIX)
  SET_TARGET_PROPERTIES( libDNN PROPERTIES OUTPUT_NAME DNN )
ENDIF(UNIX)
